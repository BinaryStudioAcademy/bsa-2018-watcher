import { SomeDirective } from './some.directive';

describe('SomeDirective', () => {
  it('should create an instance', () => {
    const directive = new SomeDirective();
    expect(directive).toBeTruthy();
  });
});
